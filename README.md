# Find Name Word App

Современное десктоп-приложение на Tkinter для поиска персонажей во втором столбце
таблиц Word-документов и формирования отчётов с подсчётом упоминаний.

## Возможности

- 📂 Выбор входного документа и отдельной папки сохранения результата
- 🧮 Автоматический подсчёт количества упоминаний персонажей и фильтрация по минимуму
- 🔢 Настройка номера столбца с персонажами прямо в интерфейсе
- 📄 Экспорт отчёта в новый `.docx` cо списком персонажей и копиями исходных таблиц
- 📋 Копирование итогового списка в буфер обмена одним кликом
- 🔡 Объединение одинаковых имён вне зависимости от регистра по желанию
- 🖥️ Удобный интерфейс с таблицей результатов, статусами и напоминаниями об ошибках

## Установка и запуск

```bash
python -m venv .venv
source .venv/bin/activate  # Для Windows используйте .venv\\Scripts\\activate
pip install -r requirements.txt
python window-strat-app.py
```

## Как работать

1. Нажмите «Выбрать документ» и укажите Word-файл `.docx` с таблицами.
2. При необходимости выберите отдельную папку сохранения.
3. Настройте минимальное количество упоминаний и номер столбца с персонажами.
4. По желанию отметьте «Игнорировать регистр имён», чтобы объединять варианты написания.
5. Нажмите «Обработать документ» и дождитесь уведомления об успешном экспорте.
6. Из таблицы в приложении можно скопировать итоговый список персонажей в буфер обмена.

## Структура проекта

```
app/
├── __init__.py        # Экспортирует функцию запуска приложения
├── gui.py             # Tkinter-интерфейс и работа с пользователем
└── processor.py       # Логика обработки документов Word
window-strat-app.py    # Точка входа
requirements.txt       # Список зависимостей
README.md
```

## Требования

- Python 3.10+
- Библиотека [python-docx](https://python-docx.readthedocs.io/)
